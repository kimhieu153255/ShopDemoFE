import React from "react";
import PropTypes from "prop-types";

const Address = ({ type }) => {
  

  return <div></div>;
};

Address.propTypes = {
  type: PropTypes.string,
};

export default Address;
